## Workstream

WS-N (link the issue)

## Files touched

- (list every file changed; should match the workstream's file list)

## Demo path verified

- [ ] `npm run dev` boots without errors
- [ ] `bash scripts/demo-offer.sh` succeeds
- [ ] `bash scripts/demo-verify.sh` succeeds
- [ ] iOS app builds (`cd ios && xcodegen generate && xcodebuild -scheme Lastscrew -destination 'platform=iOS Simulator,name=iPhone 15 Pro' build`)
- [ ] Hero demo flow runs in the simulator (`ManageItem → ReturnChooser → OfferReveal → AcceptHost → PackagingCamera → PackagingResult → HostDashboard`)

## Notes

(anything reviewers should know — risks, follow-ups, decisions taken)
